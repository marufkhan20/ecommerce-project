import DiscoverOffers from "@/components/home/DiscoverOffers";
import Slider from "@/components/home/Slider";

const Home = () => {
  return (
    <main>
      <Slider />
      <DiscoverOffers />
    </main>
  );
};

export default Home;
